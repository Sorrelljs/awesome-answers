<h1>Contact us</h1>

<!--  
Contact_path is a helper generated for us from routes. We pass the helper to the form_tag.
It associates it to the request to POST "/contact". Thi is the method and action that the for will
take when we submit the form. use <%= %> to render the form, but <% %> to end the block.
-->
<%= form_tag contact_path do%>
<div>
    <%= label_tag :name %>
    <%= text_field_tag :name %>
</div>
<div>
    <%= label_tag :email %>
    <%= email_field_tag :email %>
</div>
<div>
    <%= label_tag :message %>
    <%= text_area_tag :message %>
</div>
<%= submit_tag "Send Message" %>
<% end %>